{
  "name": "blurrysite",
  "version": "1.0.0",
  "description": "Browser extension to blur text, images, and videos for privacy",
  "scripts": {
    "test": "npm run i18n:lint && jest --coverage",
    "test:unit": "npm run i18n:lint && jest tests/unit",
    "test:e2e": "jest tests/e2e --testTimeout=30000",
    "test:watch": "jest --watch",
    "lint": "eslint src/ tests/ --ext .js",
    "i18n:lint": "node scripts/i18n_lint.js",
    "build": "node build.js"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@testing-library/dom": "^10.4.0",
    "@testing-library/jest-dom": "^6.6.3",
    "eslint": "^9.17.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "puppeteer": "^23.11.1",
    "puppeteer-core": "^23.11.1"
  }
}
